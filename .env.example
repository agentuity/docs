# Base URL for the doc-agents backend - REQUIRED
# Production: Your deployed doc-agents URL
AGENT_BASE_URL=

# Bearer token for authenticating with the agent backend - REQUIRED
# Must match the token configured in your doc-agents deployment
AGENT_BEARER_TOKEN=
