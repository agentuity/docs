# Agent Configuration
AGENT_BASE_URL=http://127.0.0.1:3500
AGENTUITY_REGION=use

# Bearer token for authenticating with the agent backend (server-side only)
# The middleware proxies /api/doc-qa, /api/sessions, /api/agent_pulse to AGENT_BASE_URL with this token
AGENT_BEARER_TOKEN=
